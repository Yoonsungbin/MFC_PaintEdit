
첫 시작시 왼쪽 클릭 에러 - > 해결
 buttondown, mousemove 에서 currentObj == NULL 일때 처리


객체 없을때 오른쪽 클릭 에러 -> 해결 
 OnRButtonUp메소드에서 OnContextMenu(this, point);
저걸 부르는데 if문을 감싸서 point가 리젼에 있는경우만 적용
			

객체선택후 다른객체 바로 그릴때 리젼이 남아있는 현상 -> 해결
 메뉴클릭시 while문으로 
obj_List에 있는모든객체의 isselected 변수를 false로 변환


색 그린 후 색변경 그 다음 폴리라인 선택해서 그리면 변경된 색 적용 -> 해결
RMenuColorButton 메소드에서 color를 지역변수 추가해서 다이얼로그에 적용된값을 여기다가 저장


doc 에 sPoint, ePoint, isSelected 다지움 -> 각 객체가 가지고 있는 변수 참조하기로

텍스트 안되는거 -> 문제 Type 이 지정되지 않았었음

광수 전달 상항 ->  OnPaint 에서
pDoc->currentObj->draw(&dc); 이게아니고 각객체의 draw를 부르는걸로 바꿈


텍스트생성후 -> 선 생성 -> 선택 -> 오른쪽클릭 에러